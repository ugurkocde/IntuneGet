# HeyboxChat 1.58.0 compatibility quarantine

Candidate `116e003e-b393-4dbc-83fe-9ec2f66d0eb1`, [run 35745461887](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35745461887), tested x64 installer SHA `C32F3FB488EC5B1FBD046DCE3098719DF2F20C270020A8F692941D5DC686DC55` with shared packager `4b4637967c6e2b0188f5713d262dd1219a02465e`. LocalSystem lifecycle returned `0/0/60001/0`. Exact captured registration `HeyboxChat` pointed to an absent `uninstall.exe` under the SYSTEM profile's `AppData\Local\Qingfeng\HeyboxChat`. Removal detection remained positive. [Run 35742782796](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35742782796) recorded the same exception before its publication failed. Fresh production state confirmed auto-pause and zero active candidates.

The [official WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/q/Qingfeng/HeyboxChat/1.58.0/Qingfeng.HeyboxChat.installer.yaml) matches the exact hash, declares machine scope and an EXE installer, uses `update` for silent installation, and names product code `HeyboxChat`. It supplies no alternate unattended removal command. The [vendor help center](https://chat.xiaoheihe.cn/home/help-center) requires JavaScript and yielded no usable removal documentation in this audit. No safe alternate executable path or removal contract was established. Local diagnostics were inaccessible; diagnosis uses bounded sanitized Actions diagnostic lines, production telemetry, and compact JSON. This evidence supports an exact-payload compatibility quarantine, not a claim that every vendor release is unsupported.

Quarantine only this immutable installer tuple through `qa_package_blocks`, consumed by QA demand and customer packaging, including override requests. Preserve the failed candidate and all security quarantines. No executable adapter or packager pin changes are needed. VirusTotal was `not_found` with null counts, so this is neither a clean security verdict nor a strict pass. Do not guess uninstall switches or delete vendor artifacts to manufacture removal.

Regression coverage verifies rejection before QA insertion, before pass reuse with either override value, and before customer GitHub Actions dispatch. Release from quarantine requires reviewed managed removal and a controlled exact-version/architecture/hash strict lifecycle with VirusTotal 0/0.

At 2026-09-22 15:28 UTC the full current-pin strict audit counted 9/500 after boundary `2026-08-30T08:28:35Z`. The status script's 588 is a status-only count, not the milestone count. Continuous supervision remains enabled.
