# WeType 2.1.4.6 exact-payload containment

Production auto-paused for candidate `fe11b347-336d-4fe7-be52-f249374d8c87`,
run `35217936430`. Fresh production and GitHub checks confirmed zero active
lifecycles and a completed failed run. The compact result records LocalSystem,
PSADT package mode, and phase exits `-1/1/60001/1`. Installation stalled after
282.535 seconds with `stalled_no_activity`. Sanitized PSADT logs show `/s`
was invoked, then no unambiguous vendor uninstall registration could be found.
Removal correctly failed closed. Final VM checkpoint restore completed.
VirusTotal was `not_found` with null verdict counts; this is not a strict pass.

## Primary evidence

- [Failed run](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35217936430)
- [Compact result](https://github.com/ugurkocde/IntuneGet-Workflows/blob/main/qa/results/tencent.wetype.json)
- [WinGet installer manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/t/Tencent/WeType/2.1.4.6/Tencent.WeType.installer.yaml):
  machine-scope EXE, `/s`, ProductCode `WeType`, exact x64 payload SHA
  `D8D487B0C3F9319B7C0A4736851701503CC662B101016CC2B62F7D657A1A41EC`.
- [WinGet locale metadata](https://github.com/microsoft/winget-pkgs/blob/master/manifests/t/Tencent/WeType/2.1.4.6/Tencent.WeType.locale.zh-CN.yaml)
  recommends the application's updater and interactive forced WinGet upgrades.
  This does not establish a corrected unattended LocalSystem install/removal route.
- Vendor site and release-note retrieval were unavailable in this cycle.

There is no verified alternate install/removal contract in this evidence.
Do not guess switches, widen identity matching, or delete artifacts to manufacture
a pass. Contain only the failed app/version/x64/SHA tuple through the existing
shared `qa_package_blocks` gate. Different versions and payloads remain eligible.
The failed candidate and compact result remain intact.

## Guarded operation

After protected merge and required CI, execute `scripts/qa-wetype-quarantine.mjs`
with `audit`, then `block`, through the existing linked production environment.
It verifies the exact failure, canonical profile hash, installer SHA, pin,
LocalSystem context, timeout, manifest arguments, and sanitized identity failure.
Only matching undispatched queued rows may be superseded. `resume` additionally
requires the exact failure pause, verified shared block, zero active lifecycles,
and matching fresh scheduler/required pins. Null reputation authorizes containment
only, never execution or a qualifying pass. Refresh enqueue before guarded resume;
audit whether the supervisor already dispatched before invoking dispatch.

The shared packager is unchanged. Required, scheduler, QA and customer workflow
pins are `bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`. The existing deployed gate
consumes this block immediately; no generator deployment or pin promotion is
needed. Regression coverage exercises normalized QA demand in both scopes,
customer override rejection, and refusal to send a customer Actions payload.

The complete production strict audit at `2026-09-17T12:10:08.882Z` returned
**42/500**, boundary `2026-08-30T08:28:35Z`, latest strict finish
`2026-09-17T11:11:01.218934+00:00`. Status-only count 402 is not authoritative
under the required current-pin and complete-evidence rules. No milestone is due.
