# SJMCL 1.3.1 exact-payload containment

Production auto-paused for candidate `23a97646-b8b4-4725-939f-a7e288bdbafa`,
run `35250591040`. Fresh production and GitHub checks confirmed zero active
lifecycles and a completed failed run. The compact result records LocalSystem,
PSADT package mode, and phase exits `0/0/60001/0`. Bounded sanitized PSADT logs
show exact captured registration `SJMCL` pointing to an absent SYSTEM-profile
LocalAppData/SJMCL/uninstall.exe. VirusTotal is `not_found`, with null
malicious/suspicious counts; it is not evidence of clean reputation.

## Primary evidence

- [Failed run](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35250591040).
- [WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/s/SJMC/SJMCL/1.3.1/SJMC.SJMCL.installer.yaml):
  NSIS, omitted scope, x64 SHA
  `D736C896A039A9AFB8B7D4339A79293FAAAC6EF3F164DAF2DD44F8702997178A`.
- [Vendor packaging source](https://github.com/UNIkeEN/SJMCL/blob/v1.3.1/src-tauri/tauri.conf.json):
  Tauri NSIS target, no explicit Windows machine scope or recovery command for
  a missing registered uninstaller.

No verified shared repair is established for this missing executable. Contain
only this exact app/version/architecture/SHA through `qa_package_blocks` with
`failed_managed_lifecycle`. Both QA demand and customer packaging consult
this gate before dispatch, including customer QA overrides. Other versions,
architectures, and installer hashes remain independently eligible.

## Guarded operation

After protected merge and required CI, run `scripts/qa-sjmcl-quarantine.mjs`
with `audit`, then `block`, using the existing linked production environment.
The script binds the failed candidate and result to the canonical profile SHA,
exact installer bytes, app identity, registered removal route, packager pin,
LocalSystem execution, phase tuple, and unverified reputation. It preserves
failed evidence and supersedes only matching undispatched queued rows.

Refresh the scheduler with the authenticated enqueue endpoint. The `resume`
action additionally requires the verified shared block, exact failure pause,
zero active lifecycles, and fresh matching scheduler/required pins. Only then
dispatch one general-queue lifecycle. SJMCL remains blocked until a reviewed
shared fix and controlled exact-payload retest establish a strict pass.

The existing production gate consumes this data immediately. No packager code
or pin change is needed; required, scheduler, QA, and customer packager pin is
`bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`. Regression coverage verifies QA
demand in both scopes, customer overrides, and customer Actions dispatch.

Strict production audit at 2026-09-17T17:19:55Z returned **46/500**, boundary
`2026-08-30T08:28:35Z`, latest strict finish `2026-09-17T15:55:50.601094Z`.
The status-only count of 409 does not satisfy the strict current-pin criteria.
No completion milestone is warranted. Continuous supervision remains enabled.
