# Orca 1.4.203 exact-payload containment

Production automatically paused after candidate
`1607ce7d-3498-448b-b9dd-d98a84b23191`, run
`34966123863`. Fresh production and GitHub checks confirmed zero active
lifecycles. The isolated LocalSystem result was Failed, with exits
`0/0/60001/0`, clean VirusTotal `0/0`, and shared packager
`6dfeaea03893e63cf7aba747638d7ea1768ac6b7`.

The exact captured NSIS registration
`2b325ec9-0ed1-575f-ad70-e08307aee879` points to
`%USERPROFILE%\AppData\Local\Programs\orca\Uninstall Orca.exe` under
SYSTEM. The generated uninstall rejected the missing executable immediately.
No managed removal occurred and detection remained positive. A marker or
registry deletion would manufacture a pass and is prohibited.

## Source review

- [Exact WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/s/StablyAI/Orca/1.4.203/StablyAI.Orca.installer.yaml):
  NSIS, omitted scope, exact captured ProductCode, installer SHA
  `DC347211CE31DC1D37BD6522B2BB96169747F626A19754C57F6868769E878A7C`.
- [Vendor release configuration](https://github.com/stablyai/orca/blob/v1.4.203/config/electron-builder.config.cjs):
  default one-click NSIS configuration, without an explicit per-machine setting.
- [Vendor uninstall hooks](https://github.com/stablyai/orca/blob/v1.4.203/config/nsis/orca-installer-hooks.nsh):
  removal is implemented in the vendor uninstaller, including daemon cleanup.

These sources do not establish a working LocalSystem removal route for the
observed missing uninstaller. Changing to user execution cannot satisfy this
cohort's LocalSystem requirement. Contain the exact payload until a reviewed
shared repair and controlled lifecycle retest establish managed removal.

## Shared containment

`scripts/qa-orca-quarantine.mjs` audits exact candidate/result identity, profile
SHA, installer SHA, pin, execution context, phase exits, and reputation. Its
`block` operation writes the existing shared `qa_package_blocks` table with
`failed_managed_lifecycle`. This blocks both QA demand and customer packaging,
including customer QA overrides. Future versions and other payloads remain
independently eligible. The original failed evidence stays intact.

Run only through the existing linked production environment. `resume` requires
the verified shared block, the exact failure pause, zero active lifecycles, and
a fresh identical required/scheduler pin. No packager code or pin changes are
needed for this existing shared gate. Do not weaken controls or retry the
blocked bytes unchanged.

## Cohort audit

At 2026-09-15T12:24:12Z the exact current-pin audit reported **9/500**
at immutable boundary `2026-08-30T08:28:35Z`. The status-only script reported
318; that is not the strict count required by this cycle. No completion
milestone should be written. The continuous service remains enabled.

The audit now also verifies explicit PSADT package mode and excludes current
app eligibility blocks, catalog exclusions, and exact blocked installer tuples.
The block for a different `astral-sh.uv` payload does not exclude its qualifying
current-pin result.
