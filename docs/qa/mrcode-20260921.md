# MrCode exact-payload containment

Production candidate `60d1fba7-2194-4278-a9b9-10980c2bbb37`, run
[35595842083](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35595842083),
failed the LocalSystem PSADT lifecycle with exits `0/0/60001/0`.
Production auto-paused and had zero active candidates at 2026-09-21T12:10:48Z.

- App/version/architecture: `zokugun.MrCode` / `1.82.0.23253` / `x64`.
- Installer SHA: `9BB0835D2F8F1F0EF8FB489B3040471BC16676DCE1670BAA8C7876A71D73EF06`.
- Profile SHA: `6402A8C3EECB3EC30E67BD4DFCA4317E9288ABB157EAB1E3FF2BD2B4446CFD50`.
- Shared packager pin: `5e561aeff961e42e63eba4fee4696c15a0cbff71`.
- VirusTotal malicious/suspicious: `0/0`.

The bounded sanitized GitHub diagnostic tail shows the exact captured Inno
uninstaller `C:\Program Files\MrCode\unins000.exe` launched with
`/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-`. Its parent exited 1 and the exact
registration `{09FF1437-F543-4CF3-9204-C4BB886DF9BE}_is1` remained through the
completion deadline. The generated package failed closed after approximately
310 seconds; independent removal detection remained positive. Exit 60001 is
the package catch result, not the original vendor exit.

The local diagnostic file was inaccessible; no ACLs were changed and the runner
working directory was not accessed. Evidence comes from the compact committed
result, production candidate/result records, and bounded sanitized job logs.

## Official metadata and disposition

[WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/z/zokugun/MrCode/1.82.0.23253/zokugun.MrCode.installer.yaml)
confirms the exact machine x64 Inno installer and hash.
[MrCode](https://github.com/zokugun/MrCode) describes its VS Code/VSCodium base.
The [matching upstream Inno source](https://github.com/microsoft/vscode/blob/1.82.0/build/win32/code.iss)
does not establish a verified replacement removal route for this failed payload.
The vendor's internal reason for exit 1 is unresolved. Do not invent switches,
force-delete files or registration, or treat this as a strict pass.

Contain the immutable tuple as `failed_managed_lifecycle` in the existing shared
`qa_package_blocks` table. Both QA admission and customer packaging already enforce
this block, including attempts to override QA. Future versions, architectures,
and different installer hashes remain independently eligible. No adapter or
packager pin change is warranted for this containment.

## Guarded operation

Run `scripts/qa-mrcode-quarantine.mjs audit`, then `block`, through the existing
linked production environment. The script validates the exact candidate, canonical
profile hash, installer hash, app tuple, pin, phase results, LocalSystem and clean
reputation before mutation. It requires the exact failure pause and zero active
lifecycles; it preserves the failed result and only supersedes matching undispatched
queued rows. `resume` additionally requires the shared block and fresh matching
scheduler pin, using a conditional control update.

Merge this containment through protected checks before running `block`. After
verification, refresh enqueue, resume, dispatch one lifecycle, and audit its phase.

The strict current-pin audit at 2026-09-21T12:13:23Z found **25/500**, latest strict
finish `2026-09-21T10:54:24.803507+00:00`, at immutable boundary
`2026-08-30T08:28:35Z`. The basic status script's 559 status-only count is not the
strict count. No milestone record is justified. The continuous supervisor remains
enabled.
