# Zoom 7.2.48358 exact-payload containment

Fresh production inspection at 2026-09-18T22:46:32Z found the automatic
failure pause, zero active candidates, and 16 queued candidates. Run
[35401931306](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35401931306)
completed cleanup and protected compact-result publication.

Candidate `352e4d5c-af3d-445f-b035-5c015186df29`, x64, installer SHA-256
`132A59637FCFF4F0F01891F163A7726976D72A4DD7199EC4C0A224CB8E28D5D1`.
Canonical profile: `E56752B592524E7FA43EA22092E0741229A6288F39934D87503AD08430A9AC98`.
Shared packager: `bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`.

The compact result and bounded sanitized PSADT logs show LocalSystem package
exits `0/0/1601/0`. Exact captured MSI product
`{e961cd8b-39eb-4a93-a78f-8008f27b17b9}` was removed with `msiexec /x`,
`REBOOT=ReallySuppress /QN`. The mutex was available, but Windows Installer
service could not be accessed. The application remained detected. VirusTotal
malicious/suspicious was `0/0`. Local diagnostic access was denied; the runner
directory was not accessed.

[Microsoft defines 1601 as an installer service initialization failure](https://learn.microsoft.com/en-us/windows/win32/msi/initialization-errors).
[Zoom supports enterprise MSI deployment](https://support.zoom.com/hc/en/article?id=zm_kb&sysparm_article=KB0064484).
The evidence does not establish a safe shared adapter correction or distinguish
a transient guest service problem from a repeatable payload issue. The existing
shared compatibility gate holds only these bytes as `failed_managed_lifecycle`.
This is not an app-wide unsupported-uninstall declaration or a security verdict.
Release requires reviewed diagnosis and a controlled strict retest; no retry is
necessary to establish the exact quarantine. Future versions stay eligible.

The current-pin SQL audit found **76/500** strict eligible distinct IDs after
`2026-08-30T08:28:35Z`, latest finish `2026-09-18T22:30:58.997441Z`.
The status-only script's 437 is not the strict milestone count.

Promotion: protected PR and checks, apply the exact migration while paused and
idle, verify the block and preserved failed result, refresh the scheduler,
verify shared workflow pins, guardedly resume, and dispatch one candidate.
The existing deployed gate consumes the row immediately. No generator change,
packager pin rotation, or old-pin queue supersession is needed.
