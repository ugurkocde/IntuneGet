# Thunder 25.1.13.1637 exact-payload containment

Production auto-paused for candidate `96aa05ec-f02d-453b-8f64-3868c997ff83`,
run `35223559453`. Fresh production and GitHub checks confirmed zero active
lifecycles and a completed failed run. The compact result records LocalSystem,
PSADT package mode, exact current shared packager pin, and phase exits
`0/0/60001/0`. Sanitized PSADT evidence shows the exact `thunder_is1` registration
remained after `Program Files/Thunder Network/Thunder/Program/ThunderUninstall.exe`
executed and the 310-second registration-removal deadline expired. Independent
removal detection still found the app. VirusTotal is `not_found`, with null
malicious/suspicious counts, and is not clean reputation evidence.

## Evidence and decision

- [Failed lifecycle](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35223559453)
- [WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/t/Thunder/Thunder/25.1.13.1637/Thunder.Thunder.installer.yaml)
  declares a machine-scope EXE, `/Silent` installation, and product code
  `thunder_is1`. It does not establish an unattended removal contract for the
  custom `ThunderUninstall.exe`; the `_is1` suffix alone is not framework proof.
- Exact x64 installer SHA:
  `B2C7A5269B267E7390BED95975FF9BA56088B26A945B6A2BA4B35B3B15FE8EC6`.
- Canonical profile SHA, independently recomputed from production:
  `409398279CAA0EEF7F7D03E0022C424302F2FFC61B2B4AC1F04D3B998FCAD87A`.

No verified adapter repair is established. Preserve the failed evidence and
contain this exact app/version/architecture/SHA through the existing shared
`qa_package_blocks` gate with `failed_managed_lifecycle`. Future versions and
different payloads remain independently eligible. Do not reuse install switches
for uninstall, delete registration/files, or interpret unknown reputation as clean.

## Guarded operation

After protected merge and required CI, execute `scripts/qa-thunder-quarantine.mjs`
with `audit`, then `block`, through the existing linked production environment.
The script verifies the precise pause, zero active lifecycles, failure result,
canonical hash, installer hash, package mode, LocalSystem, pin and bounded
registration-deadline evidence. It preserves the terminal failed candidate and
supersedes only exact matching undispatched queued rows. The `resume` action
requires a verified block, fresh matching pins, zero active lifecycles, and a
compare-and-set on the precise pause. Null reputation permits containment only.

The deployed shared gate already protects both QA and customer packaging,
including explicit QA overrides, so no generator change, new deployment, or
packager pin promotion is needed. Required, scheduler, QA and customer pins stay
`bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`. Regression tests exercise both QA scopes,
customer gate overrides and prevention of customer GitHub Actions dispatch.

At `2026-09-17T13:19:56Z`, the strict audit returned **42/500**, boundary
`2026-08-30T08:28:35Z`, latest strict finish `2026-09-17T11:11:01.218934+00:00`.
The status-only 402 is not the strict current-pin count. No milestone is due.
After containment, refresh enqueue, guard the resume and audit one active run.
