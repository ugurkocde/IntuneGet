# T3Code 0.0.42 exact-payload containment

Production auto-paused for candidate `11be0474-0a96-4ae9-ac16-ee53d6ec94aa`,
run `35214994473`. Fresh production and GitHub checks confirmed zero active
lifecycles and a completed failed run. The compact result records LocalSystem,
PSADT package mode, and phase exits `0/0/60001/0`. Bounded sanitized PSADT logs
show the exact captured registration `e9197887-efb3-55e0-985e-d6d3b5dd594a`
pointing to an absent SYSTEM LocalAppData/Programs/t3code/Uninstall T3 Code
(Alpha).exe. Managed removal could not execute. VirusTotal is `not_found`, with
null malicious/suspicious counts, and does not qualify as clean reputation.

## Primary evidence

- [Failed run](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35214994473)
- [WinGet release manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/t/T3Tools/T3Code/0.0.42/T3Tools.T3Code.installer.yaml):
  NSIS, omitted scope, exact product identity, x64 SHA
  `9BD4A00AE9B4880F85E81376844E4FC1DBC9F719120958D7445B4C2B281E267F`.
- [Vendor release packaging source](https://github.com/pingdotgg/t3code/blob/v0.0.42/scripts/build-desktop-artifact.ts):
  Windows NSIS with differentialPackage enabled; no explicit per-machine setting
  or alternative removal contract for a missing registered executable.

No verified repair for this missing executable is established. Guessing another
path, deleting files, or treating install detection as successful removal would
weaken the existing controls. Contain only this exact app/version/architecture/SHA
through `qa_package_blocks`. The existing production gate rejects this block in
QA demand and customer packaging, including explicit QA overrides. Different
versions and payloads remain independently eligible.

## Guarded operation

After protected merge and passing CI, run `scripts/qa-t3code-quarantine.mjs`
with `audit`, then `block`, via the linked production environment. The script
verifies the exact failed candidate, result, canonical profile hash, installer
SHA, pin, LocalSystem context, phase tuple, and unverified reputation before
mutation. It preserves failed evidence and supersedes only matching undispatched
queued rows. `resume` additionally requires a verified shared block, the exact
failure pause, zero active lifecycles, and fresh matching scheduler/required pins.
Null reputation is accepted only as evidence for containment, never as a pass.

The shared packager is unchanged; required, scheduler, QA and customer pins remain
`bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`. The existing production shared gate
consumes the block immediately, so no packager deployment or pin promotion is
needed. Regression cases cover normalized QA demand in both scopes, customer
gate overrides, and prevention of customer GitHub Actions payload dispatch.

The full production strict audit at 11:31:53Z returned **42/500**, boundary
`2026-08-30T08:28:35Z`, latest qualifying finish `2026-09-17T11:11:01.218934+00:00`.
The status-only count of 402 is not a strict current-pin count. No milestone
record is warranted. Resume continuous QA after verified containment and audit
the next single lifecycle.
