# Bitig 1.0.4 exact-payload containment

Fresh production evidence at 2026-09-20T14:12Z confirmed an automatic failure pause, zero active lifecycles, and 12 queued candidates. Candidate `ee5641e1-0d8e-4f73-bd95-e04f4f0a92f1`, [run 35515041627](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35515041627), completed with LocalSystem PSADT exits `0/0/60001/0`. Bounded GitHub PSADT diagnostics identify the missing registered uninstaller under `%SystemRoot%\system32\config\systemprofile\AppData\Local\Programs\bitig\Uninstall bitig.exe`. Production sanitized telemetry confirms the missing-uninstaller exception. Removal detection remained positive. The compact result records VirusTotal `not_found` with null counts: neither a strict pass nor a malware verdict.

The [official WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/b/Bitig/Bitig/1.0.4/Bitig.Bitig.installer.yaml) declares Nullsoft and SHA `1D6FBF4139EDF32FA66FC2D72151801D8D622760FAE71985A6C1167F47EFFCFF`, without an install scope. The [vendor configuration at v1.0.4](https://github.com/sametgurtuna/bitig/blob/v1.0.4/electron-builder.yml) uses assisted NSIS and does not explicitly set `perMachine`. This suggests a scope mismatch but does not establish why the registered executable is absent or prove a safe LocalSystem removal fix. Do not guess paths, change execution context to manufacture a strict pass, or delete files/registration manually. The host diagnostic file was inaccessible; investigation used the bounded GitHub and production evidence instead.

Contain only these exact version/architecture/installer bytes through existing shared `qa_package_blocks`. QA demand and customer packaging, including override requests, must reject this payload. Other versions and hashes remain eligible. Release requires reviewed diagnosis, verified reputation, and a controlled strict retest of the exact tuple.

The hash-recomputing strict audit returned **2/500** on shared pin `c861e3a90f8bbf28842b86ab0070230efecaf3a9`, boundary `2026-08-30T08:28:35Z`, latest strict finish `2026-09-20T11:30:34.617987Z`. The status-only script reports 506 but omits required evidence filters. Do not create a completion milestone.

This data-only containment uses existing shared enforcement; no packager change or pin promotion is required. Resume only after protected merge, production deployment, exact quarantine readback, fresh matching required/scheduler/QA/customer pins, and zero active lifecycles. Preserve all security quarantines and keep the supervisor enabled.
