# Pebrel 1.8.0 exact-payload containment

Candidate `f3cfef3e-7e93-4559-a3c2-23294b9132b1`, run [35504284144](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35504284144), failed with exits `0/0/60001/0`. Production automatically paused and had zero active lifecycles at the fresh audit. The exact Inno key `{61022144-7D0A-4E54-94F2-C329A8F58656}_is1` remained after 310 seconds. The captured `LocalAppData/Programs/Pebrel/unins000.exe` was invoked with `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-`.

The [version-pinned vendor source](https://github.com/Kuddev/pebrel/blob/v1.8.0/scripts/installer.iss) specifies user scope, this exact AppId, and an UninstallRun action invoking `pebrel.exe setup-ai --remove`. The [WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/k/Kuddev/Pebrel/1.8.0/Kuddev.Pebrel.installer.yaml) agrees on scope and identity. [Inno documents](https://jrsoftware.org/ishelp/topic_uninstcmdline.htm) the silent and suppress-message removal switches already used. Evidence does not establish which vendor action stalled, so do not claim a root cause or add speculative cleanup.

Quarantine only x64 version 1.8.0 with installer SHA `28FA2D4A0FFF3FA039CFFEF586875CB867020EB391DCD31BE3DB3477A8AE2159` using the existing shared `failed_managed_lifecycle` gate. Customer packaging, override requests, and QA demand must reject these bytes. Preserve the failed result and all unrelated security quarantines. Release needs a reviewed repair and a controlled lifecycle retest. This is not a malware verdict or an app-wide permanent block.

The result used User context and VirusTotal was `not_found` with null verdict counts. It is not a strict pass. The existing hash-recomputing strict audit returned **1/500** at `2026-09-20T10:34:20Z`, boundary `2026-08-30T08:28:35Z`, current shared pin `c861e3a90f8bbf28842b86ab0070230efecaf3a9`. `qa-status.mjs` reported 495 status-only IDs; that is not authoritative for this strict contract. Do not write a completion milestone.

No generator or workflow pin change is needed for this data-only shared quarantine. Resume only after the block is verified, required/scheduler/workflow pins align, the scheduler heartbeat is fresh, and active lifecycle count is zero.
