# LPub3D managed uninstall repair

Production failed candidate `552502ca-8962-4970-bc42-26c39d341c44`, run
`34929171243`, version `2.4.9.86.4133` x64. The exact installer SHA is
`FD9A7DE29FED3451258325621EB0DC3E0D056B90D4A55C694447BC223A61E4C3`.
Production auto-paused with zero active lifecycles. The tuple was 0/0/60001/0.

The bounded GitHub diagnostic shows the captured `LPub3D` registration invoked
`C:\Program Files\LPub3D\uninstall.exe /allusers /S`, then remained present for
the full removal deadline. Production live telemetry confirms that exception.
The permitted local diagnostic was access-denied; no runner directory was accessed.

The [vendor source](https://github.com/trevorsandy/lpub3d/blob/717082cdde0c7a1a34ce66cf1361c1f9ff418d18/builds/utilities/nsis-scripts/Uninstall.nsh)
sets `RunningAsShellUser` from `/shelluser`; without it, elevated callers restart
through `ExecShellAsUser` and quit. The shared application adapter appends
`/shelluser /S` to the exact registered command. It retains scope, the vendor's
single-instance checks, normal uninstall sections, and bounded exact removal.
No manual file deletion or relaxed detection is introduced.

Regression coverage: application adapter isolation, canonical QA profile,
customer GitHub payload, and executable generated PowerShell argument merging.

Operational scripts `qa-lpub3d-audit.mjs` and `qa-lpub3d-rollout.mjs` use only the
linked production environment and guard the exact project, tuple, paused/idle
state, fresh scheduler, pin, and strict retry result. Promote through protected
repair, activation, and workflow-pin PRs before using the rollout mutations.
Target enqueue with `?id=trevorsandy.lpub3d`; prioritize and dispatch its exact
retry, then pause new dispatch until its strict audit passes.

Boundary remains `2026-08-30T08:28:35Z`. At 04:53 UTC the exact-evidence audit
counted 15/500 on pin `ada1a8a5d1ad0ae9ad953306a6b528c71479a803`.
The guardian status script's 304 is a status-only count, not the strict count.
Never write the milestone from that status-only value.
