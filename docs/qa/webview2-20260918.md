# WebView2 153.0.4234.46 exact-payload containment

Candidate `ae578926-73a0-473f-b063-b3bad58bc266` failed in
[run 35291904993](https://github.com/ugurkocde/IntuneGet-Workflows/actions/runs/35291904993).
Production auto-paused; fresh production and GitHub reads confirmed no active
lifecycle. The compact result records PSADT package mode, LocalSystem, exact
installer/profile hashes, VirusTotal 0/0, and phase exits `-1/1/0/1`.
Installation stalled without activity after 409.953 seconds. Detection failed.
Uninstall followed the existing shared-runtime retention contract; its zero exit
does not establish installation success. Optional failure diagnostics could not
be preserved because the LocalSystem PowerShell Direct task timed out at 120 seconds.
The underlying install stall remains undiagnosed; do not infer an adapter defect.

[Microsoft's deployment documentation](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution)
documents the existing `/silent /install` invocation and shared Evergreen runtime.
The [official WinGet manifest](https://github.com/microsoft/winget-pkgs/blob/master/manifests/m/Microsoft/EdgeWebView2Runtime/153.0.4234.46/Microsoft.EdgeWebView2Runtime.installer.yaml)
confirms those arguments, exact x64 SHA, machine scope, and ProductCode
`Microsoft EdgeWebView`. Both x64 scopes use the same payload bytes.
No verified alternative is established by the available evidence. Preserve the
shared runtime policy, identity checks, bounded timeouts, and security checks.

Contain only Microsoft.EdgeWebView2Runtime / 153.0.4234.46 / x64 / SHA
`493AE586FF07EF3696DA3BDE3AEB73D6CACA8A1C00E779DA899FF16A159CF36E`
through the deployed shared `qa_package_blocks` gate. Different versions and
payloads remain eligible. Keep the failed candidate and result intact.

After protected merge and CI, run `scripts/qa-webview2-quarantine.mjs audit`,
then `block`, with the existing linked production environment. It revalidates
the exact failed candidate, canonical profile SHA
`5F582FFF4302DA1DBA905772777EEE3E745C5B626F52AE9DA59D009DC6E036E6`,
installer SHA, result, context, arguments, timeout, and sanitized retention log.
Only matching undispatched queued rows may be superseded. `resume` also requires
the verified shared block, exact failure pause, zero active lifecycles, and fresh
matching required/scheduler pins. Refresh enqueue, resume, then recheck active
state before dispatch because the supervisor may already have dispatched.

QA and customer workflows, required pin, and scheduler pin are all
`bc329cb8bfafd8d2af940bdc9f8ccf044ac15146`. This containment changes no generator
or application runtime code, so it requires no packager promotion. Tests cover
normalized QA demand in both scopes, customer override rejection, and refusal
to send a customer GitHub Actions payload. Release of these exact bytes requires
a reviewed repair and controlled strict retest; this containment is not a pass.

Full strict audit at 2026-09-18T07:51:57.405Z: **51/500**, immutable boundary
`2026-08-30T08:28:35Z`, latest strict finish `2026-09-17T23:30:46.175755+00:00`.
The guardian status script reports 416 from passed statuses without all strict
evidence checks; that value is not the required current-pin strict count.
No completion milestone is due. Keep the continuous supervisor enabled.
